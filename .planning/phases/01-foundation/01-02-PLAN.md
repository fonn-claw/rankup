---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/TopBar.tsx
  - src/components/Logo.tsx
  - src/components/TierBadge.tsx
  - src/components/icons/FlameIcon.tsx
  - src/components/icons/TrophyIcon.tsx
  - src/components/icons/BellIcon.tsx
  - src/components/icons/ArrowUpIcon.tsx
  - src/components/icons/ArrowDownIcon.tsx
  - src/components/icons/LightningIcon.tsx
  - src/components/icons/TargetIcon.tsx
  - src/components/icons/PhoneIcon.tsx
  - src/components/icons/CalendarIcon.tsx
  - src/components/icons/PresentationIcon.tsx
  - src/components/icons/DocumentIcon.tsx
  - src/components/icons/HandshakeIcon.tsx
  - src/components/icons/UsersIcon.tsx
  - src/app/board/layout.tsx
  - src/app/admin/layout.tsx
  - src/app/board/page.tsx
  - src/app/admin/page.tsx
autonomous: true
requirements:
  - DSGN-02
  - DSGN-03
  - DSGN-05
  - DSGN-06
must_haves:
  truths:
    - "App displays full-width layout with no sidebar on any page"
    - "Top bar is 48px tall with logo left, 3 tabs center, user info right"
    - "Tab navigation shows LIVE BOARD, BATTLES, and CHALLENGES"
    - "LIVE indicator with pulsing dot appears in the top bar"
    - "All 24 SVG/PNG assets are present in public/assets/ and functional SVGs render as React components"
    - "Tier badge component renders Gold, Silver, Bronze with correct colors"
    - "Layout is responsive -- stacked layout maintained at tablet widths, tabs remain accessible"
  artifacts:
    - path: "src/components/TopBar.tsx"
      provides: "48px navigation bar with logo, tabs, user area"
      contains: "LIVE BOARD"
    - path: "src/components/TierBadge.tsx"
      provides: "Pill-shaped tier badge with tier color"
      contains: "tier-gold\\|tier-silver\\|tier-bronze"
    - path: "src/components/icons/FlameIcon.tsx"
      provides: "Streak flame SVG icon component"
      contains: "svg"
    - path: "src/app/board/layout.tsx"
      provides: "Authenticated layout wrapping board pages with TopBar"
      contains: "TopBar"
  key_links:
    - from: "src/app/board/layout.tsx"
      to: "src/components/TopBar.tsx"
      via: "import and render"
      pattern: "import.*TopBar"
    - from: "src/components/TopBar.tsx"
      to: "src/lib/auth/actions.ts"
      via: "logout server action"
      pattern: "import.*logout"
    - from: "src/components/TopBar.tsx"
      to: "src/components/Logo.tsx"
      via: "logo rendering in top bar"
      pattern: "import.*Logo"
---

<objective>
Build the layout shell (top bar with tabs, full-width stacked layout) and create React components for all SVG icons and tier badges. Integrate the authenticated layout for /board and /admin routes.

Purpose: Delivers the visual skeleton that all Phase 2+ features render within -- the scoreboard chrome, navigation, and reusable icon/badge components.
Output: TopBar with tab navigation, authenticated layouts, 14 SVG icon components, TierBadge component, responsive full-width layout.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@DESIGN-SPEC.md

<interfaces>
<!-- From Plan 01 -- session and auth exports the executor needs -->

From src/lib/auth/session.ts:
```typescript
export interface SessionData {
  userId: number
  email: string
  name: string
  role: 'rep' | 'manager' | 'admin'
  isLoggedIn: boolean
}
export async function getSession(): Promise<IronSession<SessionData>>
```

From src/lib/auth/actions.ts:
```typescript
export async function login(formData: FormData): Promise<{ error: string } | void>
export async function logout(): never // redirects to /login
```

From src/app/globals.css (available Tailwind classes):
```
bg-bg-primary (#0A0E17), bg-bg-surface (#111827), bg-bg-surface-hover (#1A2332), bg-bg-elevated (#1E293B)
text-text-primary (#F1F5F9), text-text-secondary (#94A3B8), text-text-muted (#475569)
text-accent-cyan (#06D6F2), text-accent-green (#10B981), text-accent-red (#EF4444), text-accent-amber (#F59E0B)
text-tier-gold (#FFD700), text-tier-silver (#C0C0C0), text-tier-bronze (#CD7F32)
font-heading (Barlow Condensed), font-data (JetBrains Mono)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SVG icon components and TierBadge</name>
  <files>
    src/components/icons/FlameIcon.tsx, src/components/icons/TrophyIcon.tsx,
    src/components/icons/BellIcon.tsx, src/components/icons/ArrowUpIcon.tsx,
    src/components/icons/ArrowDownIcon.tsx, src/components/icons/LightningIcon.tsx,
    src/components/icons/TargetIcon.tsx, src/components/icons/PhoneIcon.tsx,
    src/components/icons/CalendarIcon.tsx, src/components/icons/PresentationIcon.tsx,
    src/components/icons/DocumentIcon.tsx, src/components/icons/HandshakeIcon.tsx,
    src/components/icons/UsersIcon.tsx, src/components/icons/index.ts,
    src/components/TierBadge.tsx, src/components/Logo.tsx
  </files>
  <read_first>
    public/assets/icon-flame.svg (SVG path data to extract),
    public/assets/icon-trophy.svg,
    public/assets/icon-bell.svg,
    public/assets/icon-arrow-up.svg,
    public/assets/icon-arrow-down.svg,
    public/assets/icon-lightning.svg,
    public/assets/icon-target.svg,
    public/assets/icon-phone.svg,
    public/assets/icon-calendar.svg,
    public/assets/icon-presentation.svg,
    public/assets/icon-document.svg,
    public/assets/icon-handshake.svg,
    public/assets/icon-users.svg,
    public/assets/logo.svg,
    public/assets/tier-gold.svg,
    public/assets/tier-silver.svg,
    public/assets/tier-bronze.svg,
    DESIGN-SPEC.md (component specs for TierBadge)
  </read_first>
  <action>
1. For each SVG icon file in public/assets/ (icon-flame.svg, icon-trophy.svg, icon-bell.svg, icon-arrow-up.svg, icon-arrow-down.svg, icon-lightning.svg, icon-target.svg, icon-phone.svg, icon-calendar.svg, icon-presentation.svg, icon-document.svg, icon-handshake.svg, icon-users.svg):
   - Read the SVG file content
   - Create a React component in src/components/icons/ that renders the SVG inline
   - Each component accepts `{ className?: string; size?: number }` props
   - Default size=24, use `width={size} height={size}`
   - Use `fill="currentColor"` so the icon inherits text color from Tailwind
   - viewBox should match the original SVG (likely "0 0 24 24")
   - Export the component as named export

   Pattern for each icon:
   ```tsx
   export function FlameIcon({ className = '', size = 24 }: { className?: string; size?: number }) {
     return (
       <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor" className={className}>
         {/* paste path data from public/assets/icon-flame.svg */}
       </svg>
     )
   }
   ```

2. Create `src/components/icons/index.ts` barrel export:
   ```ts
   export { FlameIcon } from './FlameIcon'
   export { TrophyIcon } from './TrophyIcon'
   export { BellIcon } from './BellIcon'
   export { ArrowUpIcon } from './ArrowUpIcon'
   export { ArrowDownIcon } from './ArrowDownIcon'
   export { LightningIcon } from './LightningIcon'
   export { TargetIcon } from './TargetIcon'
   export { PhoneIcon } from './PhoneIcon'
   export { CalendarIcon } from './CalendarIcon'
   export { PresentationIcon } from './PresentationIcon'
   export { DocumentIcon } from './DocumentIcon'
   export { HandshakeIcon } from './HandshakeIcon'
   export { UsersIcon } from './UsersIcon'
   ```

3. Create `src/components/Logo.tsx`:
   - Read public/assets/logo.svg, extract SVG markup
   - Render inline SVG with className prop
   - Default: height 32px, width auto (preserving aspect ratio from original SVG viewBox)
   - Use `fill="currentColor"` for the text parts. If the logo has a cyan accent (#06D6F2), keep that as a hardcoded fill on that specific path/element
   - Export as named: `export function Logo({ className }: { className?: string })`

4. Create `src/components/TierBadge.tsx`:
   ```tsx
   const TIER_CONFIG = {
     gold: { label: 'GOLD', textColor: 'text-tier-gold', bgColor: 'bg-tier-gold/20' },
     silver: { label: 'SILVER', textColor: 'text-tier-silver', bgColor: 'bg-tier-silver/20' },
     bronze: { label: 'BRONZE', textColor: 'text-tier-bronze', bgColor: 'bg-tier-bronze/20' },
   } as const

   type Tier = keyof typeof TIER_CONFIG

   export function TierBadge({ tier }: { tier: Tier }) {
     const config = TIER_CONFIG[tier]
     return (
       <span className={`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-heading font-semibold ${config.textColor} ${config.bgColor}`}>
         {config.label}
       </span>
     )
   }
   ```
   Per DESIGN-SPEC.md: "Pill-shaped, 20px tall. Background is tier color at 20% opacity, text is tier color at full."
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/icons/FlameIcon.tsx contains `<svg` and `fill="currentColor"` and `export function FlameIcon`
    - src/components/icons/index.ts contains exports for all 13 icons (FlameIcon through UsersIcon)
    - src/components/TierBadge.tsx contains `GOLD` and `SILVER` and `BRONZE` and `tier-gold` and `rounded-full`
    - src/components/Logo.tsx contains `<svg` and `export function Logo`
    - All 13 icon files exist in src/components/icons/
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>All 13 SVG icons converted to inline React components with className/size props using currentColor. Logo component renders logo.svg inline. TierBadge renders pill-shaped badges with tier-colored text on 20% opacity background. Barrel export in icons/index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: TopBar navigation, authenticated layouts, and responsive shell</name>
  <files>
    src/components/TopBar.tsx, src/app/board/layout.tsx, src/app/admin/layout.tsx,
    src/app/board/page.tsx, src/app/admin/page.tsx
  </files>
  <read_first>
    src/lib/auth/session.ts (getSession export, SessionData interface),
    src/lib/auth/actions.ts (logout export),
    src/components/Logo.tsx (Logo component),
    src/components/icons/index.ts (available icons),
    src/components/TierBadge.tsx (TierBadge component),
    src/app/globals.css (theme tokens),
    DESIGN-SPEC.md (TopBar spec, layout structure, screen descriptions)
  </read_first>
  <action>
1. Create `src/components/TopBar.tsx` -- a server component that reads session:
   ```tsx
   import { getSession } from '@/lib/auth/session'
   import { logout } from '@/lib/auth/actions'
   import { Logo } from '@/components/Logo'
   import Link from 'next/link'

   const TABS = [
     { label: 'LIVE BOARD', href: '/board' },
     { label: 'BATTLES', href: '/board/battles' },
     { label: 'CHALLENGES', href: '/board/challenges' },
   ] as const
   ```
   Structure (48px height, full-width, bg-bg-surface, border-b border-bg-surface-hover):
   - Left section: `<Logo className="h-6" />` (scales to 24px height)
   - Center section: 3 tab links using `font-heading text-sm font-semibold tracking-wider uppercase`
     - Active tab: `text-accent-cyan` with 2px bottom border in accent-cyan
     - Inactive tabs: `text-text-secondary hover:text-text-primary`
     - Use pathname comparison for active state (this requires a client component wrapper for usePathname, OR pass active tab as prop from layout)
     - SIMPLER: Make TopBar accept `activePath: string` prop from the layout's server component, compare against each tab href
   - Right of center tabs (but still in center area): "LIVE" indicator -- small pulsing green dot (8px circle, `bg-accent-green`, CSS animation `animate-pulse`) followed by text "LIVE" in `text-accent-green text-xs font-heading font-semibold`
   - Right section: User name (`font-data text-sm text-text-secondary`) + logout form button (`text-text-muted hover:text-text-primary text-xs font-data`)
     - Logout: `<form action={logout}><button type="submit">Logout</button></form>`
     - Streak badge placeholder area (empty span, populated in Phase 2)

   Responsive behavior:
   - Desktop (md+): Full layout as described
   - Tablet/mobile (<md): Logo + hamburger OR tabs stack below. Simplest: tabs shrink text, remove "LIVE" text (keep dot), abbreviate user name. Keep all elements visible but compact.
   - Use Tailwind responsive: `hidden md:flex` for less critical elements, `text-xs md:text-sm` for font scaling

2. Create `src/app/board/layout.tsx` -- authenticated layout for /board routes:
   ```tsx
   import { getSession } from '@/lib/auth/session'
   import { redirect } from 'next/navigation'
   import { TopBar } from '@/components/TopBar'

   export default async function BoardLayout({ children }: { children: React.ReactNode }) {
     const session = await getSession()
     if (!session.isLoggedIn) redirect('/login')

     return (
       <div className="min-h-screen bg-bg-primary flex flex-col">
         <TopBar activePath="/board" />
         <main className="flex-1 w-full">
           {children}
         </main>
       </div>
     )
   }
   ```
   The layout checks auth and renders TopBar + children. Full-width, no sidebar. Main fills remaining viewport height.

3. Create `src/app/admin/layout.tsx` -- same pattern but with admin role check:
   ```tsx
   import { getSession } from '@/lib/auth/session'
   import { redirect } from 'next/navigation'
   import { TopBar } from '@/components/TopBar'

   export default async function AdminLayout({ children }: { children: React.ReactNode }) {
     const session = await getSession()
     if (!session.isLoggedIn) redirect('/login')
     if (session.role !== 'admin') redirect('/board')

     return (
       <div className="min-h-screen bg-bg-primary flex flex-col">
         <TopBar activePath="/admin" />
         <main className="flex-1 w-full">
           {children}
         </main>
       </div>
     )
   }
   ```

4. Update `src/app/board/page.tsx` to show a proper placeholder with design system styling:
   - Remove the auth check (layout handles it now)
   - Show: "LIVE BOARD" heading in font-heading text-4xl text-accent-cyan
   - Show: "YOUR POSITION" banner placeholder (bg-bg-surface, rounded, p-6, border-l-4 border-accent-cyan)
     - Inside: session.name, session.role badge, "Scoreboard coming in Phase 2" in text-text-muted
   - Show: "RANKINGS" section placeholder -- 5 mock rows using bg-bg-surface/bg-bg-primary alternating, 64px tall, showing the empty-board.svg asset centered
   - This demonstrates the design system is working: dark theme, correct fonts, correct colors, full-width layout

5. Update `src/app/admin/page.tsx` similarly:
   - Remove auth check (layout handles it)
   - Show: "ADMIN" heading in font-heading text-4xl text-accent-cyan
   - Show: "Cross-team configuration coming in Phase 3" in text-text-muted
   - Show placeholder cards (bg-bg-surface, rounded) for "Teams", "Activity Config", "Engagement" using font-heading for titles

6. Add the pulsing dot animation to globals.css if Tailwind's built-in `animate-pulse` isn't sufficient:
   The built-in `animate-pulse` should work for the green LIVE dot. If a more specific "glow pulse" is needed, add:
   ```css
   @keyframes live-pulse {
     0%, 100% { opacity: 1; box-shadow: 0 0 4px var(--color-accent-green); }
     50% { opacity: 0.5; box-shadow: 0 0 8px var(--color-accent-green); }
   }
   ```
   and reference via inline style or a custom utility.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TopBar.tsx contains `LIVE BOARD` and `BATTLES` and `CHALLENGES`
    - src/components/TopBar.tsx contains `h-12` or `h-[48px]` (48px height)
    - src/components/TopBar.tsx contains `animate-pulse` or `live-pulse` (pulsing LIVE dot)
    - src/components/TopBar.tsx contains `logout` import/usage
    - src/components/TopBar.tsx contains `Logo` import
    - src/app/board/layout.tsx contains `import.*TopBar` and `getSession` and `redirect('/login')`
    - src/app/board/layout.tsx does NOT contain `sidebar` or `aside` (full-width, no sidebar)
    - src/app/admin/layout.tsx contains `role !== 'admin'` and `redirect('/board')`
    - src/app/board/page.tsx contains `font-heading` and `text-accent-cyan` and `bg-bg-surface`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>TopBar renders at 48px with logo, 3 tab navigation (LIVE BOARD | BATTLES | CHALLENGES), pulsing LIVE dot, user name, and logout button. Board and admin layouts wrap children with TopBar and auth checks. Full-width stacked layout with no sidebar. Placeholder pages demonstrate design system (dark theme, correct fonts, correct colors). Responsive at tablet widths.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- 13 SVG icon components exist in src/components/icons/ with barrel export
- TierBadge renders 3 tiers with correct colors
- TopBar is 48px, has 3 tabs, pulsing LIVE dot, logout button
- No sidebar element exists anywhere in the layout
- Board layout redirects unauthenticated users
- Admin layout redirects non-admin users
- All 24 assets in public/assets/ (unchanged, verified by ls count)
- `ls public/assets/ | wc -l` returns 24
</verification>

<success_criteria>
- Build passes with zero errors
- TopBar visible on /board and /admin with correct navigation tabs
- Full-width layout with no sidebar on any page
- 13 inline SVG icon components render with currentColor inheritance
- TierBadge shows Gold/Silver/Bronze with metallic colors on 20% opacity backgrounds
- LIVE indicator pulses green in top bar
- Layout responds to tablet viewport (768px) without breaking
- Logout button in TopBar destroys session
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
