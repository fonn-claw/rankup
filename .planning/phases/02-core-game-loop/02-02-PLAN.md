---
phase: 02-core-game-loop
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/board/page.tsx
  - src/app/board/BoardClient.tsx
  - src/app/board/ScoreboardRow.tsx
  - src/app/board/PinnedPosition.tsx
  - src/app/board/FilterBar.tsx
  - src/app/board/ActivityFAB.tsx
  - src/app/board/DealBellStack.tsx
  - src/app/board/ConversionFunnel.tsx
autonomous: true
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04
  - LEAD-05
  - LEAD-06
  - ACTV-01
  - ACTV-03
  - ACTV-05
  - TIER-02
  - TIER-03
  - STRK-02
  - STRK-03
  - STRK-04
  - BELL-01
  - BELL-02
  - BELL-03
  - FUNL-01

must_haves:
  truths:
    - "Rep sees ranked list of all reps with rank, name, tier badge, XP bar, daily change, and streak"
    - "Rep sees their pinned position with gap-to-next-rank in actionable terms"
    - "Rep can log any activity type via FAB and sees XP update optimistically"
    - "Leaderboard filters by time period and team via pill toggles"
    - "Deal bell notification slides down showing rep name, deal info, and XP"
    - "Active streaks show flame with day count, broken streaks show muted, at-risk shows pulsing amber"
    - "Top 3 rows have visual emphasis with tier-colored left border glow"
  artifacts:
    - path: "src/app/board/page.tsx"
      provides: "Server component fetching leaderboard, streaks, funnel data"
      min_lines: 20
    - path: "src/app/board/BoardClient.tsx"
      provides: "Client component with polling, optimistic state, scoreboard layout"
      min_lines: 60
    - path: "src/app/board/ScoreboardRow.tsx"
      provides: "64px scoreboard row with rank, movement, name, tier, XP bar, change, streak"
      min_lines: 30
    - path: "src/app/board/PinnedPosition.tsx"
      provides: "96px pinned banner with gap-to-next-rank message"
      min_lines: 25
    - path: "src/app/board/FilterBar.tsx"
      provides: "Pill toggle filters for time period and team"
      min_lines: 20
    - path: "src/app/board/ActivityFAB.tsx"
      provides: "Floating action button with slide-up activity panel"
      min_lines: 50
    - path: "src/app/board/DealBellStack.tsx"
      provides: "Deal bell notification stack with polling and auto-dismiss"
      min_lines: 40
    - path: "src/app/board/ConversionFunnel.tsx"
      provides: "Per-rep conversion funnel with ratios"
      min_lines: 15
  key_links:
    - from: "src/app/board/page.tsx"
      to: "src/lib/queries/leaderboard.ts"
      via: "getLeaderboard() server call"
      pattern: "getLeaderboard"
    - from: "src/app/board/BoardClient.tsx"
      to: "src/lib/hooks/usePolling.ts"
      via: "usePolling(15000) for live refresh"
      pattern: "usePolling\\(15000\\)"
    - from: "src/app/board/ActivityFAB.tsx"
      to: "src/lib/actions/activities.ts"
      via: "logActivity server action with useOptimistic"
      pattern: "logActivity"
    - from: "src/app/board/DealBellStack.tsx"
      to: "/api/deal-bell"
      via: "fetch polling every 15s"
      pattern: "fetch.*deal-bell"
    - from: "src/app/board/ScoreboardRow.tsx"
      to: "src/components/TierBadge.tsx"
      via: "TierBadge component import"
      pattern: "TierBadge"
---

<objective>
Build the complete scoreboard UI: board page with server-fetched data, client component with polling and optimistic updates, scoreboard rows with all columns, pinned position banner with gap-to-next, filter bar, floating action button with slide-up panel, deal bell notifications, streak indicators, and conversion funnel.

Purpose: This is THE product -- the live scoreboard that makes reps compete. Every visual element from the design spec is implemented here.

Output: 8 files comprising the full interactive scoreboard experience.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-game-loop/02-CONTEXT.md
@.planning/phases/02-core-game-loop/02-RESEARCH.md
@.planning/phases/02-core-game-loop/02-01-SUMMARY.md

@DESIGN-SPEC.md
@src/app/board/layout.tsx
@src/app/globals.css
@src/components/TopBar.tsx
@src/components/TierBadge.tsx
@src/components/icons/FlameIcon.tsx
@src/components/icons/BellIcon.tsx
@src/components/icons/ArrowUpIcon.tsx
@src/components/icons/ArrowDownIcon.tsx
@src/components/icons/PhoneIcon.tsx
@src/components/icons/CalendarIcon.tsx
@src/components/icons/PresentationIcon.tsx
@src/components/icons/DocumentIcon.tsx
@src/components/icons/HandshakeIcon.tsx

<interfaces>
<!-- Contracts created by Plan 02-01 that this plan consumes -->

From src/lib/queries/leaderboard.ts:
```typescript
export interface LeaderboardRow {
  userId: number
  name: string
  teamId: number | null
  teamSlug: string | null
  totalXp: number
  todayXp: number
  rank: number
  prevRank: number | null
}
export function getLeaderboard(opts: { period: 'today' | 'week' | 'month' | 'all', teamId: number | null }): Promise<LeaderboardRow[]>
export function getPreviousRanks(opts: { period: 'today' | 'week' | 'month' | 'all', teamId: number | null }): Promise<Map<number, number> | null>
export function getGapMessage(currentXP: number, nextRankXP: number, nextRankPosition: number): string
export function getTier(xp: number): 'gold' | 'silver' | 'bronze'
```

From src/lib/queries/streaks.ts:
```typescript
export interface StreakResult {
  userId: number
  currentStreak: number
  streakActive: boolean
}
export function getStreaks(): Promise<StreakResult[]>
```

From src/lib/queries/funnel.ts:
```typescript
export interface FunnelResult {
  userId: number
  calls: number
  meetings: number
  demos: number
  deals: number
}
export function getFunnel(userId?: number): Promise<FunnelResult[]>
```

From src/lib/actions/activities.ts:
```typescript
export function logActivity(type: 'call' | 'meeting' | 'demo' | 'proposal' | 'deal', metadata?: string): Promise<{ success: true, xp: number, type: string }>
export type ActivityType = keyof typeof XP_VALUES
```

From src/lib/hooks/usePolling.ts:
```typescript
export function usePolling(intervalMs: number): void
```

From src/lib/constants.ts:
```typescript
export const XP_VALUES = { call: 10, meeting: 50, demo: 75, proposal: 100, deal: 500 } as const
export const TIER_THRESHOLDS = { bronze: { min: 0, max: 4999 }, silver: { min: 5000, max: 14999 }, gold: { min: 15000, max: Infinity } } as const
export const ACTIVITY_STREAK_THRESHOLD = 15
```

From src/lib/auth/session.ts:
```typescript
export interface SessionData { userId: number; email: string; name: string; role: 'rep' | 'manager' | 'admin'; isLoggedIn: boolean }
```

From src/components/TierBadge.tsx:
```typescript
export function TierBadge({ tier }: { tier: 'gold' | 'silver' | 'bronze' }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board page, BoardClient, ScoreboardRow, and PinnedPosition</name>
  <files>src/app/board/page.tsx, src/app/board/BoardClient.tsx, src/app/board/ScoreboardRow.tsx, src/app/board/PinnedPosition.tsx, src/app/board/ConversionFunnel.tsx</files>
  <action>
**Replace** `src/app/board/page.tsx` (currently a placeholder):

1. Server Component. Import `getLeaderboard`, `getPreviousRanks`, `getStreaks`, `getFunnel`, `getSession`.
2. Fetch default view: `getLeaderboard({ period: 'week', teamId: null })`, `getPreviousRanks({ period: 'week', teamId: null })`, `getStreaks()`, `getFunnel()`.
3. Merge previous ranks into leaderboard rows by matching userId.
4. Merge streak data into leaderboard rows by matching userId.
5. Pass all data to `<BoardClient>` as props: `initialRows`, `initialStreaks`, `currentUserId: session.userId`, `currentUserName: session.name`, `funnelData`.
6. Also fetch teams list via simple `db.select().from(teams)` for the filter bar.

**Create** `src/app/board/BoardClient.tsx`:

1. `'use client'` component. Props: `initialRows` (leaderboard rows with prevRank and streak merged), `currentUserId`, `currentUserName`, `teams` (for filter), `funnelData`.
2. Call `usePolling(15000)` for 15-second refresh.
3. Use `useOptimistic` with reducer pattern: `(rows, { userId, xpGain }) => rows.map(r => r.userId === userId ? { ...r, totalXp: r.totalXp + xpGain, todayXp: r.todayXp + xpGain } : r).sort((a, b) => b.totalXp - a.totalXp).map((r, i) => ({ ...r, rank: i + 1 }))`. This re-sorts and re-ranks on optimistic update.
4. State for active filters: `period` (default 'week') and `teamId` (default null). Since changing filters needs a server re-fetch, use `startTransition` with `router.push('/board?period=X&team=Y')` to trigger server component re-render with new params. OR simpler: use searchParams in page.tsx to drive filter state (pass period/teamId from URL to getLeaderboard). The BoardClient manages the filter UI and navigates. Choose the searchParams approach -- it works with server components and polling naturally.
5. **Update page.tsx to read searchParams:** `searchParams.period` and `searchParams.team` to pass to getLeaderboard. Default to 'week' and null.
6. Layout structure inside BoardClient:
   - `<DealBellStack />` (fixed position, top of viewport below TopBar)
   - `<FilterBar>` with current period/teamId and teams list
   - `<PinnedPosition>` for current user
   - Scrollable scoreboard: `<div className="space-y-0">` with `initialRows.map(row => <ScoreboardRow key={row.userId} ... />)`
   - `<ActivityFAB>` (fixed position, bottom-right)

**Create** `src/app/board/ScoreboardRow.tsx`:

1. `'use client'` (needed for hover states and future animation prep).
2. Props: `rank: number, userId: number, name: string, tier: 'gold' | 'silver' | 'bronze', totalXp: number, todayXp: number, prevRank: number | null, streak: { count: number, active: boolean } | null, isTop3: boolean, isCurrentUser: boolean`.
3. 64px height (`h-16`), full-width flex row.
4. Alternating background: `rank % 2 === 0 ? 'bg-bg-primary' : 'bg-bg-surface'`. If `isCurrentUser`, add subtle `bg-accent-cyan/5` tint.
5. Columns left to right:
   - **Rank**: `font-heading text-[28px] font-bold` in 48px wide column. Text color: `text-text-primary`.
   - **Movement arrow**: 16px column. Compare rank vs prevRank. If prevRank is null or equal, show dash in `text-text-muted`. If rank < prevRank (improved), show `<ArrowUpIcon className="w-4 h-4 text-accent-green" />`. If rank > prevRank (dropped), show `<ArrowDownIcon className="w-4 h-4 text-accent-red" />`.
   - **Name**: `font-data text-sm text-text-primary` with avatar placeholder (colored circle with first initial, 28px). Flex row with gap-2.
   - **Tier badge**: `<TierBadge tier={tier} />`.
   - **XP bar + number**: Container 120px wide. Bar track: `bg-bg-primary rounded-full h-2`. Fill: `bg-accent-cyan rounded-full h-2` with width as percentage of max XP on the board (pass maxXp as prop or compute). Below bar: `font-data text-xs text-text-secondary` showing `totalXp.toLocaleString()` + " XP".
   - **Today's change**: Right-aligned `font-data text-sm`. If todayXp > 0: `text-accent-green` showing `+${todayXp}`. If 0: `text-text-muted` showing `—`.
   - **Streak icon**: If streak exists and streak.active: `<FlameIcon className="w-5 h-5 text-accent-amber" />` + `<span className="font-data text-xs text-accent-amber">${streak.count}d</span>`. If streak exists but not active (broken): `<FlameIcon className="w-5 h-5 text-text-muted opacity-50" />` + `<span className="font-data text-xs text-text-muted line-through">${streak.count}d</span>`. If no streak data: empty.
   - **Streak at risk (STRK-04)**: If current user, streak is active, todayXp from calls is 0 (or < ACTIVITY_STREAK_THRESHOLD calls today -- approximate with todayXp check), and current hour >= 12 UTC, show pulsing amber: `animate-pulse text-accent-amber`.
6. Top 3 emphasis (LEAD-05): If `isTop3`, add `border-l-2` with border color matching tier: `style={{ borderLeftColor: tier === 'gold' ? '#FFD700' : tier === 'silver' ? '#C0C0C0' : '#CD7F32' }}`. Add subtle `shadow-[inset_0_0_20px_rgba(6,214,242,0.05)]` for glow effect.
7. Use `data-user-id={userId}` attribute for Phase 4 animation prep. Key by userId in parent map.

**Create** `src/app/board/PinnedPosition.tsx`:

1. Client component (`'use client'`).
2. Props: `rank: number, name: string, totalXp: number, todayXp: number, tier: 'gold' | 'silver' | 'bronze', streak: { count: number, active: boolean } | null, gapMessage: string | null, funnelData: FunnelResult | null`.
3. 96px tall (`h-24`), `bg-bg-surface` with gradient to transparent at bottom, `border-l-4 border-accent-cyan`.
4. Layout: Two rows inside.
   - Top row: Rank number (large, `font-heading text-3xl font-bold`), Name, TierBadge, XP total (`font-data text-lg`), Today's change.
   - Bottom row: Gap message in `text-accent-amber font-data text-sm` (e.g., "2 calls to pass #6"), streak badge, and conversion funnel summary if funnelData exists.
5. Gap message: Import `getGapMessage` and compute in the parent (BoardClient), pass as string prop. If user is rank 1, show "You're #1!" in `text-accent-cyan`.
6. Funnel summary (FUNL-01): If funnelData, show compact inline: `{calls} -> {meetings} -> {demos} -> {deals}` with conversion percentages. Use `<ConversionFunnel>` sub-component.

**Create** `src/app/board/ConversionFunnel.tsx`:

1. Props: `calls: number, meetings: number, demos: number, deals: number`.
2. Compact inline display: `font-data text-xs text-text-secondary`.
3. Show: `{calls}C -> {meetings}M ({pct}%) -> {demos}D ({pct}%) -> {deals}W ({pct}%)`.
4. Conversion percentages: meetings/calls, demos/meetings, deals/demos. Handle divide-by-zero gracefully (show "—").
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/rankup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Board page fetches real data via server component and renders a full scoreboard with: ranked rows showing rank number, movement arrows, name, tier badge, XP bar, daily change, and streak indicator. Current user's position is pinned at top with gap-to-next-rank in actionable terms. Top 3 rows have tier-colored left border glow. Conversion funnel shows per-rep ratios. Leaderboard refreshes every 15 seconds via usePolling. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: FilterBar, ActivityFAB, and DealBellStack</name>
  <files>src/app/board/FilterBar.tsx, src/app/board/ActivityFAB.tsx, src/app/board/DealBellStack.tsx</files>
  <action>
**Create** `src/app/board/FilterBar.tsx`:

1. `'use client'` component.
2. Props: `currentPeriod: string, currentTeamId: number | null, teams: { id: number, name: string, slug: string }[]`.
3. Two rows of pill toggles inline above the scoreboard.
4. **Time period pills**: Today, This Week, This Month, All Time. Each is a link (`next/link`) to `/board?period=today|week|month|all&team={currentTeamId || ''}`. Active pill: `bg-accent-cyan text-bg-primary font-semibold`. Inactive: `bg-bg-surface text-text-secondary hover:bg-bg-surface-hover`.
5. **Team pills**: All, then each team name from props. Link to `/board?period={currentPeriod}&team={teamId || ''}`. Same active/inactive styling.
6. Pill styling: `px-3 py-1 rounded-full font-data text-xs transition-colors`.
7. Both rows in a single `flex flex-wrap gap-2 items-center` container. Separate period and team groups with a thin `border-r border-bg-surface-hover` divider or just spacing.

**Create** `src/app/board/ActivityFAB.tsx`:

1. `'use client'` component.
2. Props: `currentUserId: number, currentXp: number, onOptimisticUpdate: (xpGain: number) => void`.
3. State: `isOpen` (boolean) for panel visibility, `isLogging` (boolean) for loading state, `lastLogged` (null | { type, xp }) for success indicator.
4. **FAB button**: Fixed `bottom-6 right-6`, 56px cyan circle (`w-14 h-14 rounded-full bg-accent-cyan`), centered `+` or `<LightningIcon>` in `text-bg-primary`. `z-40`. On click, toggle `isOpen`.
5. **Slide-up panel**: When open, fixed at bottom, covering bottom 40% of viewport (`bottom-0 left-0 right-0 h-[40vh]`). `bg-bg-elevated` with rounded top corners. `z-30`. Transition: `translate-y-full` when closed, `translate-y-0` when open, with `transition-transform duration-300 ease-out`.
6. Panel content: Header "LOG ACTIVITY" in `font-heading text-sm text-text-secondary tracking-wider`. Grid of 5 activity buttons (2 columns + 1):
   - Call: `<PhoneIcon />` + "Call" + "+10 XP" — `bg-bg-surface hover:bg-bg-surface-hover`
   - Meeting: `<CalendarIcon />` + "Meeting" + "+50 XP"
   - Demo: `<PresentationIcon />` + "Demo" + "+75 XP"
   - Proposal: `<DocumentIcon />` + "Proposal" + "+100 XP"
   - Deal: `<HandshakeIcon />` + "Deal" + "+500 XP" — this button has special styling: `bg-accent-green/10 border border-accent-green/30`
7. Each button: tall, icon on top, label below, XP value in `text-accent-cyan font-data text-xs`. On click: call `handleLog(type)`.
8. **Deal flow**: When Deal button is clicked, show an additional input for deal name and revenue amount before submitting. Simple inline form: text input for deal name, number input for revenue. Submit button. Use `metadata: JSON.stringify({ dealName, revenue })`.
9. **handleLog function**:
   - Call `onOptimisticUpdate(XP_VALUES[type])` for immediate parent state update.
   - Call `logActivity(type, metadata)` server action (imported).
   - On success: set `lastLogged` to show brief success indicator (green checkmark + XP earned).
   - After 1.5 seconds, clear `lastLogged` and close panel via `setTimeout`.
   - On error: show error toast (simple inline error message in panel), revert is handled automatically by useOptimistic in parent.
10. **Activity log panel XP display (ACTV-05)**: Each button clearly shows XP value.

**Create** `src/app/board/DealBellStack.tsx`:

1. `'use client'` component. No props needed (self-contained polling).
2. State: `bells` (array of deal notifications), `seenIds` (Set of activity IDs already shown).
3. **Polling**: `useEffect` with `setInterval` every 15 seconds. Fetch `/api/deal-bell`. Filter out already-seen IDs. Prepend new deals to `bells` array. Add their IDs to `seenIds`.
4. **Auto-dismiss**: Each bell auto-removes after 8 seconds. Use individual timers per bell via `useEffect` that sets a timeout for each new bell. Alternatively, track `showAt` timestamp per bell and use a single interval to prune expired ones.
5. **Layout**: Fixed position `top-12 left-0 right-0 z-50` (below TopBar). `flex flex-col gap-1`.
6. **Bell component**: Each bell is a full-width bar. `bg-bg-elevated border-l-4 border-accent-green px-4 py-3`. Content: `<BellIcon className="w-5 h-5 text-accent-green" />` + text: `"{repName} closed {dealName} — ${amount.toLocaleString()} — +{xpEarned} XP"` in `font-data text-sm text-text-primary`.
7. **Background texture**: Apply `bg-deal-bell.png` as subtle background via `style={{ backgroundImage: 'url(/assets/bg-deal-bell.png)', backgroundSize: 'cover', backgroundPosition: 'center' }}` with low opacity overlay.
8. **Stacking**: Newest on top. If multiple bells, they stack vertically with 4px gap. Max 3 visible at once (older ones removed).
9. **Entry animation**: Simple CSS transition: `translate-y` from `-100%` to `0` with `transition-transform duration-300`.
10. **Tier promotion detection (TIER-03)**: In BoardClient, when optimistic update causes XP to cross a tier threshold (compare pre-update tier vs post-update tier using getTier), briefly flash the TierBadge or show a small celebration message in the PinnedPosition banner ("Promoted to Silver!"). This is a client-side check, no server involvement.

**Wire everything in BoardClient.tsx**:

1. Import FilterBar, PinnedPosition, ScoreboardRow, ActivityFAB, DealBellStack, ConversionFunnel.
2. Compute `gapMessage` for current user: find their row and the row above them in sorted order, call `getGapMessage()`.
3. Pass `onOptimisticUpdate` to ActivityFAB that calls the `useOptimistic` setter.
4. Compute `maxXp` from rows for XP bar percentage in ScoreboardRow.
5. Compute current user's streak at-risk status for ScoreboardRow.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/rankup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>FilterBar renders pill toggles for time period and team, navigating via URL params. ActivityFAB shows 56px cyan button that opens slide-up panel with 5 activity types showing XP values, deal option includes revenue input, logging triggers optimistic XP update and server persist. DealBellStack polls /api/deal-bell every 15s, shows notification bars that auto-dismiss after 8s and stack. Tier promotion is detected client-side. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Board page at /board renders scoreboard with real leaderboard data
- Scoreboard rows show: rank number, movement arrow, name, tier badge, XP bar, daily change, streak
- Pinned position shows current user with gap-to-next-rank
- Top 3 rows have tier-colored left border glow
- Filter pills change time period and team, triggering data re-fetch
- FAB opens slide-up panel, logging activity updates XP optimistically
- Deal bell notifications appear when deals exist in last 30 seconds
- Streak indicators: flame for active, muted for broken, pulsing for at-risk
- Conversion funnel shows per-rep ratios
- Key prop on ScoreboardRow is userId (not rank) for Phase 4 animation prep
</verification>

<success_criteria>
1. Rep sees full ranked leaderboard with all specified columns (LEAD-01)
2. Pinned position shows gap-to-next in actionable terms like "2 calls to pass #6" (LEAD-02)
3. Time period and team pill filters work and update the scoreboard (LEAD-03, LEAD-04)
4. Top 3 rows have tier-colored left border glow (LEAD-05)
5. Rank movement arrows show up/down/stable based on previous period comparison (LEAD-06)
6. FAB opens slide-up panel with 5 activity types showing XP values (ACTV-01, ACTV-05)
7. XP updates optimistically in real-time after logging (ACTV-03)
8. Tier badges display on scoreboard rows (TIER-02)
9. Tier promotion detected and celebrated on threshold crossing (TIER-03)
10. Active streaks show flame + count, broken show muted, at-risk show pulsing amber (STRK-02, STRK-03, STRK-04)
11. Deal bell slides down from top with rep name, deal info, XP; auto-dismisses 8s; stacks (BELL-01, BELL-02, BELL-03)
12. Per-rep conversion funnel with ratios displayed (FUNL-01)
13. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-game-loop/02-02-SUMMARY.md`
</output>
